# TASK-015 Planner Agent Prompt

## Status
todo

## Priority
high

## Objective
Create a Planner prompt that turns repository analysis and MVP goals into prioritized, implementation-ready backlog tasks.

## Scope
- Add or update `prompts/agents/planner.md` with explicit Planner responsibilities.
- Define a deterministic task selection and creation rule set aligned with `AGENTS.md`.
- Require output formatting for new/updated tasks using `backlog/tasks/TASK-TEMPLATE.md` and `docs/agent-handoff-contract.md`.
- Include a compact example showing how the Planner transforms analysis findings into 2–3 concrete tasks.

## Out of Scope
- Implementing any backlog task generated by the Planner.
- Building automatic backlog prioritization tooling.
- Rewriting all existing backlog items.

## Implementation Notes
Focus on practical backlog quality: clear scope boundaries, concrete acceptance criteria, and explicit dependencies.

## Acceptance Criteria
- `prompts/agents/planner.md` exists (or is updated) with clear, role-specific instructions.
- The prompt enforces creation of small, implementation-ready tasks with explicit dependencies.
- The prompt output format is compatible with `docs/agent-handoff-contract.md`.

## Dependencies
- TASK-010
- TASK-011
